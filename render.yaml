services:
  - type: web
    name: youtube-summarizer
    env: python
    rootDir: subdomain-yt-supervan-uk
    buildCommand: pip install --no-cache-dir --force-reinstall --upgrade -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
